{
  "name": "ditar-generador-codigo",
  "version": "1.0.0",
  "private": true,
  "description": "Ditar SAS · Generador de Código de Producto (Nomenclatura PT v1.0), con catálogo de campos servido desde Supabase.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx --yes serve public"
  }
}
